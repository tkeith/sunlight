services:

  node:
    restart: "no"
    build:
      context: ../docker-contexts/node
    volumes:
      - ..:/opt/app
      - ../.cache/runtime-config.json:/opt/runtime-config.json
      - ${deployment_state_filename}:/opt/deployment.json
      - node_modules:/opt/app/node_modules
